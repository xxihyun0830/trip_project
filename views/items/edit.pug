extends ../layout

block content
  .container
    h1.main-title Edit Tour Item
    form(action=`/items/${item.id}`, method="POST")
      input(type="hidden", name="_method", value="PUT")
      include _form
      button.btn.btn-outline-primary(type="submit") Update Your Tour Item
